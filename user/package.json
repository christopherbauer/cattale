{
	"name": "user",
	"version": "1.0.0",
	"main": "index.ts",
	"scripts": {
		"start": "ts-node-dev index.ts --respawn",
		"build": "tsc",
		"build:fresh": "npm uninstall @cattale/server && npm i ../core/server",
		"build:test": "npm run build && node dist/index.js",
		"check": "tsc --noEmit",
		"test": "jest",
		"test:watch": "jest --watchAll"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"description": "",
	"dependencies": {
		"@cattale/server": "file:../core/server",
		"express": "4.18.2",
		"express-oauth2-jwt-bearer": "1.3.0",
		"ts-node-dev": "2.0.0"
	},
	"devDependencies": {
		"@types/express": "4.17.17"
	}
}
