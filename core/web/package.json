{
	"name": "@cattale/web",
	"version": "1.0.0",
	"description": "Core library code that is shared between services",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": [
		"/dist"
	],
	"scripts": {
		"build": "tsc",
		"build:fresh": "rm -rf dist && npm run build",
		"build:test": "npm run build && node dist/index.js",
		"check": "tsc --noEmit",
		"test": "jest",
		"test:watch": "jest --watchAll"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"typescript": "5.0.2"
	},
	"devDependencies": {
		"jest": "29.5.0"
	}
}
